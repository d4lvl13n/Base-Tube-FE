export interface SignInWeb3Props {
  // Currently no props, but good to have for future
}

export interface FeatureCardProps {
  icon: React.ReactNode;
  title: string;
  description: string;
  className?: string;
}

export interface WalletSectionProps {
  className?: string;
} 